/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['montrion.local', 'montrion.designhu-demo.com'],
  },
};

module.exports = nextConfig;
